# Firebase API keys are public client identifiers, not secrets.
# They are embedded in every copy of the Aqualisa Android APK.
# See: https://firebase.google.com/docs/projects/api-keys
secret:
  ignored-paths:
    - "custom_components/aqualisa/const.py"
